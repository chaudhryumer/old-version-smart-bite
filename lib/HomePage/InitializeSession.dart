
import 'package:flutter/cupertino.dart';

class InitializeSession extends StatefulWidget {
  const InitializeSession({super.key});

  @override
  State<InitializeSession> createState() => _InitializeSessionState();
}

class _InitializeSessionState extends State<InitializeSession> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
